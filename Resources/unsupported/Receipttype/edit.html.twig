{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar tipo de recibo</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('receipttype') }}">
            Regresar a la lista de tipos de recibo
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
