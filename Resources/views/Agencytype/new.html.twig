{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Crear un nuevo tipo de Agencia</h1>

    {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('agencytype') }}">
            Regresar a la lista
        </a>
    </li>
</ul>
{% endblock %}
