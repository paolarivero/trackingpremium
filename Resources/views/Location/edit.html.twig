{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar Ubicación en Bodega</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('location') }}">
            Regresar a la lista de ubicaciones
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
