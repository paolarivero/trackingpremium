{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar COD</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('cod') }}">
            Regresar a la lista de COD
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
