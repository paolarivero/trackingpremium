{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Nuevo COD</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('cod') }}">
            Regresar a la lista de COD
        </a>
    </li>
</ul>
{% endblock %}
