{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar tipo de cliente</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('customertype') }}">
            Regresar a la lista de tipos de clientes
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
