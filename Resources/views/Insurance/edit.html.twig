{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar seguro de agencia</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('insurance') }}">
            Regresar a la lista de seguros de agencia
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
