{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar tipo de status de un cliente</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('customerstatus') }}">
            Regresar a la lista de status de clientes
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
