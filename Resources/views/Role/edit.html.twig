{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Edición de un Rol</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('role') }}">
            Regresar a la lista de roles
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
