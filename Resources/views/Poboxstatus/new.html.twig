{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Nuevo status de casillero</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('poboxstatus') }}">
            Regresar a la lista de status de casillero
        </a>
    </li>
</ul>
{% endblock %}
