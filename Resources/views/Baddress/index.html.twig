{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Baddress list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Lastname</th>
                <th>Address</th>
                <th>Phone</th>
                <th>Mobile</th>
                <th>Barrio</th>
                <th>Zip</th>
                <th>Docid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('baddress_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.lastname }}</td>
                <td>{{ entity.address }}</td>
                <td>{{ entity.phone }}</td>
                <td>{{ entity.mobile }}</td>
                <td>{{ entity.barrio }}</td>
                <td>{{ entity.zip }}</td>
                <td>{{ entity.docid }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('baddress_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('baddress_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('baddress_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
