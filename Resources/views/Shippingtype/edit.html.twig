{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Modificar tipo de envío</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('shippingtype') }}">
            Regresar a la lista de Tipos de Envío
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
