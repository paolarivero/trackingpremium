{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Noticias</h1>

    <table class="records_list">
        <thead>
            <tr>
{#                <th>Id</th> #}
                <th>Título</th>
                <th>Contenido</th>
                <th>Fecha</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                {# <td> 
		<a href="{{ path('news_show', { 'id': entity.id }) }}"> 
		 {{ entity.id }} </a></td> #}
                <td>{{ entity.title }}</td>
                <td>{{ entity.content }}</td>
                <td>{% if entity.creationdate %}{{ entity.creationdate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('news_show', { 'id': entity.id }) }}">Mostrar</a>
                    </li>
                    <li>
                        <a href="{{ path('news_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('news_new') }}">
                Nueva noticia
            </a>
        </li>
    </ul>
    {% endblock %}
