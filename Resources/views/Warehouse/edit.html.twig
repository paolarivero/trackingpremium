{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Modificar bodega</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('warehouse') }}">
            Regresar a la lista de Bodegas
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
