{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar impuesto de adencia</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('tax') }}">
            Regresar a la lista de impuestos de agencia
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
