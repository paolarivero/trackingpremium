{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar oficina</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('office') }}">
            Regresar a la lista de oficinas
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
