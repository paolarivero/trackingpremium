{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Nueva oficina</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('office') }}">
            Regresar a la lista de oficinas
        </a>
    </li>
</ul>
{% endblock %}
